#!/usr/bin/env python3
"""Run the guided installer directly from a Cairn source checkout."""

from __future__ import annotations

import sys
from pathlib import Path

SOURCE = Path(__file__).resolve().parent
sys.path.insert(0, str(SOURCE / "src"))

from cairn_install.cli import main  # noqa: E402

raise SystemExit(main(default_source=SOURCE))
